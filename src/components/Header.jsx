import hero from "../../src/img/hero.png"

const Header = () => {
    return (
        <header className="center">
            <img src={hero} alt="" />
        </header>
    )
}
export default Header